# WIP Audit — `stash@{0}` 完整解读

**Date**: 2026-09-22
**Source**: `git stash list` → `stash@{0}: On main: wip-bugfixes-2026-09-22-pre-P0`
**Stats**: 61 files changed, **+4,091 / −1,075**
**Author**: stash @ main (HEAD = `81d45c3`)

---

## ⚠️ 重要更正

上一轮对话里我把这 61 个文件叫"bug fix WIP"——**那是错的**。这是
**完整的产品功能增量 + 可靠性优化包**，主题非常清晰，与"bug fix"无关。

---

## 📊 改动统计（按主题分组）

### 主线改动（按代码行数）

| # | 模块 | 文件 | 行数 | 主题 |
|---|---|---|---|---|
| 1 | `emailing/store.py` | 1 | +536 / -137 | email 存储重构 + 3 张新表 |
| 2 | `agents/email_craft_agent.py` | 1 | +552 / -X | 邮件生成重构 + 内容安全 |
| 3 | `emailing/graph_client.py` | 1 | +374 / -X | Graph OAuth 多账号重构 |
| 4 | `api/routes.py` | 1 | +450 / -X | hunt/manual API 路由 |
| 5 | `agents/lead_extract_agent.py` | 1 | +307 / -X | lead 提取 + 接收新 lead |
| 6 | `api/app.py` | 1 | +225 / -X | 主入口 + checkpoint 清理 |
| 7 | `api/hunt_store.py` | 1 | +157 / -X | lead 去重 + 持久化 |
| 8 | `agents/lead_identity.py` | 1 | +110 / -X | identity 提取 |
| 9 | `emailing/scheduler.py` | 1 | +98 / -X | scheduler 限流增强 |
| 10 | `emailing/reply_detector.py` | 1 | +84 / -X | reply 检测幂等 |
| 11 | `emailing/readiness.py` | 1 | +77 / -X | 邮箱 readiness |
| 12 | `api/unsubscribe_routes.py` | 1 | +70 / -X | 退订管理 UI |
| 13 | `api/email_routes.py` | 1 | +65 / -X | email API |
| 14 | `tools/web_search.py` | 1 | +63 / -X | API key 容错 |
| 15 | `api/email_accounts_routes.py` | 1 | +59 / -X | 多账号 CRUD |
| 16 | `tools/contact_extractor.py` | 1 | +50 / -X | 手机号规范化 |
| 17 | `automation_routes.py` | 1 | +46 / -X | automation API |
| 18 | `requirements.txt` | 1 | +1 | `+phonenumbers` |
| ... | ... | ... | ... | ... |
| 19 | `graph/evaluate.py` | 1 | +53 / -X | decision 函数 |
| 20 | `tests/` | 16 files | +932 / -195 | 测试覆盖 |

总计 **61 文件、+4,091 / −1,075 行**。

### 二进制改动

- `payment-qr.jpeg`: 77150 → **0 bytes**（清空/删除）
- `wechat-qr.jpeg`: 132284 → **0 bytes**（清空/删除）

⚠️ **如果 stash pop 时图片没恢复，会影响 README 渲染**。

---

## 🎯 11 大功能主题

### 1. **手动邮件发送**（manual_send）

**新表**：`manual_send_recipient_claims`
- `emailing/store.py`:
  - `release_manual_send_preparation`
  - `list_manual_send_history(hunt_id)`
  - `mark_message_sent`
- `api/routes.py`:
  - `_evict_excess_hunts()` — hunt 数量限制
  - `_restore_manual_send_history(...)`
  - `_sent_draft_response(...)` — draft → sent

**用户故事**：AI 找完客户后，营销人员手动确认后发送，而不是自动发。

### 2. **入站邮件管理**（inbox / unread）

**新表**：`email_inbound_messages` + `email_inbound_sync_state`
- `emailing/store.py` 新增 8 个方法：
  - `upsert_inbound_message(payload, now_iso)`
  - `update_inbound_message(message_id, **fields)`
  - `list_inbound_messages(...)`
  - `get_inbound_message(message_id)`
  - `count_inbound_unread(owner_user_id, enforce_owner)`
  - `mark_inbound_site_read(message_id, is_read, now_iso)`

**用户故事**：收件箱 UI 持久化、未读计数、按 owner 隔离。

### 3. **退订管理 UI**（unsubscribe CRUD）

- `emailing/store.py`: `search_unsubscribes` / `delete_unsubscribe` / `get_unsubscribe`
- `api/unsubscribe_routes.py`: **+3 个 endpoint**（GET/POST/DELETE）
- 新增测试：`tests/test_email/test_unsubscribe.py`（+116 行）
- 新增测试：`tests/test_email/test_html_format.py`（+28 行）

**用户故事**：运营人员可搜索/查看/删除退订记录（合规审计）。

### 4. **Lead 去重与持久化重构**

- `api/hunt_store.py` 新增：
  - `hunt_data_lock()` — 文件锁（hunt JSON 并发写保护）
  - `current_leads(hunt)` / `current_lead_keys()` — 取出当前 lead
  - `_dedup_hunt_paths()` — 候选文件路径
  - `saved_leads(hunt_id)` — 已保存的 lead
  - `accept_new_leads(hunt_id, leads)` — 接收并去重
  - `purge_old_hunts(retention_days)` — 数据保留清理
- `agents/lead_extract_agent.py` 导入：
  - `from api.hunt_store import accept_new_leads, current_lead_keys`
- `scripts/dedupe_hunt_leads.py` + `scripts/standalone_dedupe.py` + `scripts/clean_weak_identity_keys.py`

**用户故事**：lead 不再无限增长、有去重、有保留期。

### 5. **租约心跳 + SQLite 并发**

- `automation/job_queue.py` +39：
  - `PRAGMA journal_mode = WAL`（读写并发）
  - `PRAGMA busy_timeout = 10000`（10s 锁等待）
  - **`touch_heartbeat(job_id, claim_token, worker_id)`** — 长任务心跳
  - requeue 重置 `attempt_count = 0`（手动重试是新预算）
- `emailing/store.py` 同样加 WAL + busy_timeout

**用户故事**：长 hunt 不被 lease 误杀、SQLite 不再频繁"database is locked"。

### 6. **Graph API 多账号 OAuth**

`emailing/graph_client.py` +374 重大重构：

| 旧 | 新 |
|---|---|
| `class GraphClient` 单例 | 拆成 `_global_creds()` + `_account_creds(account)` |
| `async def get_token(self)` | `async def get_token(self, account=None)` |
| `fetch_graph_replies(...)` 单账号 | `fetch_graph_inbox_messages(...)` 新接口 + `fetch_graph_replies` 重构 |
| — | `distinct_poll_accounts(...)` 新函数（多账号轮询） |
| — | `representative_identity(account)` 新函数 |
| — | `class GraphNetworkError(RuntimeError)` 新异常类型 |

**用户故事**：每个 email account 独立 OAuth、独立轮询。

### 7. **邮件内容安全（policy enforcement）**

`agents/email_craft_agent.py` +552：

| 新增函数 | 作用 |
|---|---|
| `_configured_sender_contacts(settings)` | 取出已配置的 sender 联系方式 |
| `_email_content_policy_hits(text, settings)` | 检测违规内容（电话/链接/LinkedIn/邮箱等）|
| `_sanitize_sender_signature(sig, settings)` | 清理发件人签名 |
| `_sanitize_email_sequence_content(...)` | 清理整个 sequence 内容 |

| 新增 regex | 用途 |
|---|---|
| `_BRACKET_PLACEHOLDER_RE` | 占位符 `[]` |
| `_EMAIL_ADDRESS_RE` | 邮箱地址 |
| `_URL_RE` | URL |
| `_LINKEDIN_RE` | LinkedIn 提及 |
| `_CONTACT_LABEL_RE` | 联系方式标签 |
| `_PHONE_RE` | 电话号码 |

**用户故事**：AI 生成的邮件不会被插入敏感联系方式（避免绕过模板）。

### 8. **手机号规范化**

`requirements.txt`: `+phonenumbers>=8.13.0,<9.0.0`

`tools/contact_extractor.py` +50：
- `+_phone_region_hint(country_code, address, website)` — 推断地区
- `+normalize_phone_number(phone, region)` — E.164 规范化
- 修改 `extract_phone_numbers(text, country_code="")` — 增加地区参数
- 修改 `sanitize_phone_list(phones, region)` — 增加地区参数

**用户故事**：lead 提取的电话号码变成 E.164 标准格式。

### 9. **Web 搜索 API Key 容错**

`tools/web_search.py` +63：
- `+_is_key_failure(exc)` — 判断 Key 死信
- `+mark_dead(key, cooldown_seconds=600)` — 标记死信 10 分钟
- `+is_dead(key)` — 查询

**用户故事**：单个 Key 触发 rate limit / quota 后，10 分钟内不再用，避开连锁失败。

### 10. **Hunt 状态机增强**

- `graph/state.py`: `+low_yield_rounds: int`（连续低产轮次计数）
- `app.py`:
  - `_cleanup_orphan_checkpoints()` — 清理孤儿 checkpoint
  - `_hunt_exists(thread_id)` — hunt 是否存在
  - `_cancel_check_with_heartbeat()` — 取消检查 + 心跳
- `graph/evaluate.py` +53：决策函数增强

**用户故事**：避免"低产轮次"无限循环；孤儿 checkpoint 不再泄漏。

### 11. **多邮箱 readiness + scheduler 限流**

- `emailing/readiness.py` +77：邮箱账号 readiness 状态机
- `emailing/scheduler.py` +98：限流/退避增强
- `emailing/reply_detector.py` +84：reply 检测幂等

**用户故事**：邮箱账号不可用时自动跳过；reply 检测不重复处理同一 message_id。

---

## ⚠️ 风险评估

### 高风险点（stash pop 前需准备）

1. **二进制文件 0 字节**：`payment-qr.jpeg` / `wechat-qr.jpeg` 已清空
   - 风险：README 渲染显示空图片
   - 处理：stash pop 后检查图片是否被恢复（可能 git 留着旧版，新版是 0 字节）

2. **`test_lead_identity.py` -119 行大改**（删了大量测试）
   - 风险：可能丢覆盖率
   - 处理：看具体删了什么；如果是 refactor（合并测试）OK，如果是删覆盖率需补

3. **`test_lead_extract_agent.py` +208 / -111 行大改**
   - 风险：测试假设改变
   - 处理：与 baseline 7 个失败对照看是否相关

4. **`emailing/store.py` +536 + `email_craft_agent.py` +552 同时大改**
   - 风险：cross-file 耦合（`accept_new_leads` 在 hunt_store 但 lead_extract 用）
   - 处理：跑完整测试集确认

5. **Graph Client API 变了**：`get_token(account=...)` 等新签名
   - 风险：scheduler / reply_detector 调用处未跟改会 broken
   - 处理：grep 所有 `get_token(` 调用点

6. **PRAGMA WAL 在多处独立设置**（`store.py` + `job_queue.py`）
   - 风险：与 P0.3 migration framework 严重冲突（已撤回 P0.3，OK）
   - 处理：无（已避开）

### 低风险点（通常 OK）

- 测试文件 +932/-195：测试覆盖增加
- `requirements.txt` +1：phonenumbers 库稳定
- `config/settings.py` +26：可能是新配置字段

---

## 📋 决策建议（怎么 stash pop）

### 方案 1：分批 pop（最稳，**推荐**）

```bash
# 把 stash 拆成多个 patch，按主题 cherry-pick
git stash show -p stash@{0} -- backend/emailing/store.py > /tmp/store.patch
git stash show -p stash@{0} -- backend/api/hunt_store.py > /tmp/hunt_store.patch
git stash show -p stash@{0} -- backend/agents/lead_extract_agent.py > /tmp/lead_extract.patch
# ...

# 按依赖顺序逐个 apply + 跑测试
git apply /tmp/store.patch  # 先 store（schema 层）
uv run pytest tests/test_email/  # 验证 store
git apply /tmp/hunt_store.patch  # 再 hunt_store
git apply /tmp/lead_extract.patch  # 再 lead_extract
uv run pytest tests/test_agents/test_lead_extract_agent.py
```

**优势**：每个主题独立可回滚。
**劣势**：耗时（每个 patch 都要验证）。

### 方案 2：全量 pop + 修 baseline 失败（中等）

```bash
cd /Users/yan/Desktop/project/AI Hunter/FInd_Customer
git stash pop  # 一次性 pop 全部
git stash drop  # 或保留 stash 备用
uv run pytest  # 跑全部测试
# 修 7 个 baseline 失败
uv run pytest  # 再次跑确认绿
git add -A
git commit -m "feat: manual send + inbox + unsubscribe + lead dedup + heartbeat + multi-account"
```

**优势**：快，与 stash 原意一致。
**劣势**：万一有回归，需 `git reset` 或 `git stash` 反悔。

### 方案 3：先 commit stash pop 后再 cherry-pick P0（混合）

```bash
# 1. pop stash
cd /Users/yan/Desktop/project/AI Hunter/FInd_Customer
git stash pop
git stash drop  # pop 后 stash 已 pop 到 working tree
# 修测试 + commit WIP

# 2. 再 cherry-pick P0（已撤回 P0.3，只剩 P0.1+P0.2）
git fetch ../AI-Hunter-p0 p0/implementation:p0/implementation
git checkout main  # 或 worktree
git merge p0/implementation  # fast-forward 到 P0.1+P0.2
# 解决可能的冲突
```

**优势**：每步 commit 都干净。
**劣势**：复杂度高。

---

## 🎯 我的建议

**走方案 2（全量 pop + 修 baseline 失败）**，原因：

1. stash 主题统一（都是 WIP 内容），不存在"P0 + WIP 混合作战"的混乱
2. P0.1 + P0.2 已经是 clean commit（无需冲突解决）
3. 修 7 个 baseline 失败是 stash 内部的修改，原状态好处理
4. 修完后建议用 `git add -p` 按主题分多个 commit（而不是一个大 commit）

**等用户决策后再动手**——我不会主动 `git stash pop`。

---

## 📝 关联文档

- `docs/pipeline-flow-review.md` — 上一轮的流程审查报告（任务链路 + 邮件生成链路）
- `.ai-memory/20260922/P0_plan.md` — P0 实施前规划（已完成）
- `docs/P0-implementation-summary.md` — P0.1+P0.2 交付总结

---

**Author**: 编程专家 (P8 全栈) skill 审计
**Status**: ✅ Audit complete. **Awaiting user decision** on which方案 to execute next.